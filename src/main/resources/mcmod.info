[
  {
    "modid": "${modid}",
    "name": "${modName}",
    "description": "${modDescription}",
    "version": "${modVersion}",
    "mcversion": "${minecraft}",
    "authorList": ["${modAuthors}"],
    "url": "${url}",
    "dependencies": [
      "${modDeps}"
    ]
  }
]